# Development dependencies for Code Navigator.
#
# Single source of truth: the [dev] extra in pyproject.toml (pinned in uv.lock).
# This file just defers to it so the dev toolchain can never drift out of sync
# with pyproject / the lockfile (it previously duplicated the list and had gone
# stale — e.g. isort/flake8, which the project replaced with ruff).
#
#   pip install -r dev-requirements.txt      # equivalent to the line below
#   pip install -e ".[dev]"

-e .[dev]
